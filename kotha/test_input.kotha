main function {
    dekhaw("Enter a number: ");
    purno age;
    nao(age);
    
    dekhaw("You entered: ");
    dekhaw(age);
    
    purno doubled = age * 2;
    dekhaw("Doubled: ");
    dekhaw(doubled);
}
