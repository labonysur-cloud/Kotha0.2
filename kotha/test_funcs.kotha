kaj add(a, b) {
    ferot a + b;
}

main function {
    dekhaw("Functions:");
    dhoro sum = add(10, 20);
    dekhaw(sum);

    dekhaw("Arrays:");
    talika arr[5];
    arr[0] = 100;
    arr[1] = 200;
    dekhaw(arr[0]);
    dekhaw(arr[1]);
}
