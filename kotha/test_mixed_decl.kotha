main function {
    // Test 3: Mixed initialization
    purno x = 10, y, z = 30;
    y = 20;
    
    doshomik pi = 3.14, e = 2.71, phi;
    phi = 1.618;
    
    dekhaw("Integers:");
    dekhaw(x);
    dekhaw(y);
    dekhaw(z);
    
    dekhaw("Floats:");
    dekhaw(pi);
    dekhaw(e);
    dekhaw(phi);
}
