main function {
    dekhaw("=== Kotha User Input Demo ===");
    
    dekhaw("Enter your age: ");
    purno age;
    nao(age);
    
    dekhaw("You are: ");
    dekhaw(age);
    
    purno nextYear = age + 1;
    dekhaw("Next year you will be: ");
    dekhaw(nextYear);
}
