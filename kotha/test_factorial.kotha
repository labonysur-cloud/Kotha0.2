// Test factorial function
kaj factorial(n) {
    jodi (n <= 1) {
        ferot 1;
    } othoba {
        purno temp = n - 1;
        purno rec = factorial(temp);
        purno result = n * rec;
        ferot result;
    }
}

main function {
    purno result = factorial(5);
    dekhaw(result);
}
